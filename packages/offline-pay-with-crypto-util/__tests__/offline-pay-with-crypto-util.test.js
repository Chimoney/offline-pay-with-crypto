'use strict'

const offlinePayWithCryptoUtil = require('../lib/offline-pay-with-crypto-util')

describe('offline-pay-with-crypto-util', () => {
  it('needs tests`', () => {
    expect(1).toBe(1)
  })
})
